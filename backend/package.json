{
  "name": "bubble-penetration-backend",
  "version": "1.0.3",
  "description": "Microservice to exchange highscores globally",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "prestart": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "start": "node src/app.js | pino-pretty",
    "start:prod": "npm run prestart && npm run migrate:deploy && node src/app.js"
  },
  "author": "Lukas Scheerer",
  "license": "ISC",
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "body-parser": "^2.3.0",
    "dotenv": "^17.2.2",
    "express": "^5.2.1",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3",
    "prisma": "^7.9.1"
  }
}
